# Runtime-only dependencies for AWS Lambda container
# Excludes: matplotlib, pandas, ipykernel, jupyter, pytest and other dev/notebook packages

# Core RAG stack
langchain==0.3.25
langchain-openai==0.3.16
langchain-pinecone==0.2.4
langchain-community==0.3.24

# Vector DB
pinecone==6.0.2

# API layer
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
mangum==0.21.0

# Document processing
beautifulsoup4==4.13.4
requests==2.32.3
lxml==5.4.0

# Utilities
python-dotenv==1.1.0
tiktoken==0.9.0
tqdm==4.67.1

# Pin numpy to 1.x — 2.x wheels use manylinux_2_28 which requires glibc 2.28+,
# but the Lambda Amazon Linux 2 base only provides glibc 2.17 (manylinux2014).
numpy==1.26.4
